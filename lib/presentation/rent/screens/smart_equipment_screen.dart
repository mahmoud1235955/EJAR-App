import 'package:flutter/material.dart';

class SmartEquipmentScreen extends StatelessWidget {
  const SmartEquipmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}